FROM node:22-alpine

WORKDIR /app

COPY package.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["sh", "-c", "node -e \"require('rollup')\" 2>/dev/null || (rm -rf node_modules && npm ci); npm run dev"]
